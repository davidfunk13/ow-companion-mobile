import { StyleSheet, } from "react-native";

const styles = StyleSheet.create({
	spinnerContainer: {
		padding:    10,
		height:     100,
		display:    "flex",
		alignItems: "center", 
	},
});

export default styles;
